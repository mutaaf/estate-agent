## What problem does this solve?

<!-- Not what it changes — what was wrong. -->

## If it touches detection or resolution

```
python3 tests/run_all.py --report
```

- Precision before / after:
- Recall before / after:
- **What did you check to make sure it does not find things that are not there?**

Precision may not regress. Recall may improve.

## Checklist

- [ ] `python3 tests/run_all.py` passes
- [ ] No new dependency, no network code
- [ ] Nothing identifying added (names, hosts, real credentials)
- [ ] Docs updated if behaviour changed
